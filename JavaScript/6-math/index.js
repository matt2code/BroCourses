let x = 3.14;
let y = 5;
let z = 9;
let maximum;
let minimum;

//x = Math.round(x); //rounds number
//x = Math.floor(x); //rounds number DOWN
//x = Math.ceil(x); //rounds number UP
//x = Math.pow(x, 3); //exponent
//x = Math.sqrt(x); //square root
//x = Math.abs(x); //absolute (turns negative to positive)

//maximum = Math.max(x, y, z); //bigger number
//minimum = Math.min(x, y, z); //smaller number

x = Math.PI //ratio of the circumference of a circle

document.getElementById("displayValue").innerHTML = x;